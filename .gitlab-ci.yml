stages:
  - build
  - test
maven_build:
  stage: test
  tags:
    - automation
    - frontend
    - qa
  script:
    - echo "Build project with maven"
    - apt install -y xorg-x11-server-Xvfb
    - mvn clean test -DSuites/testngStaging=testngStaging.xml
