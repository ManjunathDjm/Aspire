stages:
  - build
  - test
maven_build:
  stage: test
  tags:
    - automation
    - frontend
    - qa
  script:
    - echo "Build project with maven"
    - xvfb-run --auto-servernum --server-args='-screen 0, 1600x900x24' yarn cover
    - mvn clean test -DSuites/testngStaging=testngStaging.xml
